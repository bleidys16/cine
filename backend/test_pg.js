const {Pool} = require('pg'); const p = new Pool({connectionString: require('dotenv').config().parsed.DATABASE_URL}); p.query(\SELECT codigo, EXTRACT(EPOCH FROM ((CURRENT_TIMESTAMP AT TIME ZONE 'America/Bogota') - (f.fecha::date + f.hora::time))) / 60 AS min_desde_inicio FROM tiquetes t JOIN funciones f ON f.id=t.funcion_id WHERE t.codigo='3FRGEGW0TZ'\).then(r => console.log(r.rows)).finally(()=>p.end());
